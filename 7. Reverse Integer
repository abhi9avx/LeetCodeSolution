#Python_Solution

class Solution:
    def reverse(self, x: int) -> int:
        t=str(x)
        if x<0:
            t=t[1:]
        t=int(t[::-1])
        if t>=(2**31)-1 : return 0
        elif x<0 :return "-" +str(t)
        else : return t
